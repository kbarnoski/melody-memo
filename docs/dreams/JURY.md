# Concept Jury Verdict — 2026-09-09

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified catalog; mic/camera/MIDI/pose/tilt/pen/keyboard/touch/data/peer only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (e1e3a05→0d1eea1). Read-only on `src/app/dream/**` — no prototype touched. Since the 2026-09-08 verdict, TWO fires: **c1227 shipped `17104-firstlook`** (the lure that verdict's #4 named — not a new concept, a glance-preview of inkpressure) and **c1228 held** (research-only, banked **zero** seeds — that verdict's #3 executed exactly). Both did precisely what the last verdict asked. Window rotated by one: `tonguescript` (16656) aged out, `firstlook` (17104) entered; bottom of window is now `scriptorium` (16672). RESEARCH.md fresh (2026-09-09 entries). The ceiling is still `0 / 15 / 0`, now ~12 days flat. **This verdict is deliberately shorter than the last — enforcing my own 2026-09-08 #3 ("hold harder, not wider" applies to the jury too).**_

## Summary
The lab read the last verdict perfectly and did both things it asked: built the one legitimate lure (`firstlook`), then held and built nothing rather than pile a tenth un-opened piece. Both were correct, and neither moved the ceiling — still `0 / 15 / 0`, ~12 days. That is now the whole finding. Every lever — renderer, concept, ambition, restraint, and now a purpose-built lure — has been pulled cleanly, and all of them are orthogonal to the only number that matters: a human opening one page. The one honest new note is that following the last verdict had a small cost: `firstlook`, by faithfully copying inkpressure (as any true preview must), pushed cool-luminous to 5× and gave the lab a *second* front door to the same single ask.

## Diversity audit
_(last 15, numerically highest on disk: scriptorium, albumvoyage, cadencemap, chordnebula, harmonicswarm, attune, gushbody, tunevoice, strata, nodalfigure, tensethread, vaultloom, inkpressure, voiceloom, firstlook — `tonguescript` aged out; `firstlook` entered)_

- **Over-represented input:** **`pointer/touch drag` — 5×** (chordnebula, harmonicswarm, strata, nodalfigure, tensethread — the same five, unchanged, all older work). Still ban-floored, still a trailing tail: the three newest ships (inkpressure, voiceloom, firstlook) are all fully autonomous, zero pointer. **`keyboard/typing` dropped to 3×** (scriptorium, albumvoyage, cadencemap — `tonguescript` aged out) — **no longer over-represented; that ban releases.** Healthy: autonomous/passive **3×** (the good heading), camera/mic/peer/gyro 1× each.
- **Over-represented output:** **nothing at ≥4.** `DOM/typographic` dropped to **3×** (manuscripts, `tonguescript` gone) — **ban releases.** Three renderers now tie at **3× (WATCH, do not make any of them 4×): `three.js`** (chordnebula, tensethread, vaultloom), **`raw WebGL2`** (attune, gushbody, tunevoice), and **`SVG vector ink`** — which climbed from 2× to 3× because `firstlook` is a third SVG piece (nodalfigure, inkpressure, firstlook). `WebGPU` 2×, `Canvas2D` **1×** (voiceloom; c1225 used it, several fires back now — effectively rested again), **`audio-only` 0×** (the most-rested output, still 0× for the whole stall).
- **Over-represented technique:** **`chord consonance/tension mapping` — ~5×** (harmonicswarm, tunevoice, nodalfigure, tensethread + chordnebula's chord→colour cousin). Unchanged, no sixth added — keep it a tail. `prosody/text-score` dropped to **3×** (manuscripts) — **ban releases.** Singleton escape-routes to protect: causal voice-leading (voiceloom), variable-weight velocity-ink (inkpressure/firstlook, now 2× by design), ruled-surface/growth-front (vaultloom), cross-session stratigraphy (strata), GPU particle-life (harmonicswarm), optical flow (gushbody), co-presence (attune), volumetric raymarch (chordnebula).
- **Over-represented vibe:** **`cool luminous` — 5× (UP from 4×)** (strata, nodalfigure, vaultloom, inkpressure, **+firstlook**). This is the one tag that got *worse*, and it got worse for a defensible reason: a faithful preview of inkpressure must reuse its palette. Still: banned next cycle. `near-dark/intimate typographic` dropped to **3×** (manuscripts) — **ban releases.** `warm/ember` 2× (attune, gushbody; voiceloom's warm-neutral adjacent) — WATCH. `bright cosmic` 2× — WATCH.
- **BANNED for next cycle:** `pointer/touch drag input` (5×) · `chord consonance/tension technique` (~5×) · `cool luminous palette` (5×). **RELEASED this rotation (dropped to 3×, no longer floored):** `keyboard/typing input` · `DOM/typographic output` · `prosody/text-score technique` · `near-dark/intimate typographic palette` — the entire manuscript-block quartet unlocked in one aging-out. **WATCH (3×, do not push to 4×):** `three.js`, `raw WebGL2`, `SVG vector ink`. **Most-rested outputs: `audio-only` (0×) and `Canvas2D` (1×, now rested again).**

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Empty floor, as it has been all stall. Stop watching it.
- **Hit 2–3 criteria — 15 — all exactly 3.** `firstlook` is the newest: **#2** (subsystems — real-track analysis load + recency-weighted pressure envelope + auto-passage-selection + variable-width SVG bake + optional synced audio) + **#3** (Calliphony / CHI-2026 / ASAP, cited) + **#5** (RESEARCH <14d). But score it honestly: it is a *deliberate duplicate* of inkpressure, not an ambition play — a lure clearing the middle of the bar, by design.
- **Hit 4–5 criteria — 0.** **`0 / 15 / 0`, ~12 days flat — the thirteenth consecutive verdict to write this line.** Since the concept-break began the lab has cashed nine distinct fresh categories, declined-#1 honestly eleven-plus straight, diversified on command, held on command, and built a targeted lure on command. The top band has never once been non-zero. Every one of the fifteen is stamped "not run in this env — needs Karel at the page."

## Standouts (positive)
- **17104-firstlook** — the correct build, well made. It is the *only* legitimate build inside the immutability constraint that targets the actual gate, and it out-executes its own brief: it auto-selects the single most dynamically-contrasty ~12 s passage (max pressure range), reveals it at 3× muted with no press-play and no permission, and works in silence because width is pure geometry. It is now a **better glance-verdict than inkpressure itself** — inkpressure asks you to press play and watch 15 s; firstlook is already looping when the page opens. If one thing gets opened, this is now the stronger door.
- **c1228's hold (zero seeds)** — not a slug, the best judgment call of the window. The previous verdict warned that a *second* research fire must bank zero new directions or become a "restraint pile." c1228 banked zero and spent its budget sharpening the one queued pick (breathline, now with a concrete ~0.36 Hz target). Exactly right.
- **17024-inkpressure** — still the reference eye-verdict; now the "full piece" behind the firstlook door rather than the front door itself.
- **16848-tunevoice** — still the fastest ears-verdict (~20 s auto-demo, mic optional). Eye (firstlook→inkpressure) and ears (tunevoice) remain the whole game.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **The two front doors are now a two-door problem.** inkpressure and firstlook ask for the *identical* verdict. Presenting both as co-equal links splits the one tap. Pick **one** top door (firstlook — it's the better glance) and let inkpressure be strictly downstream. Do not build a *third* preview under any framing — one lure was the legitimate build; a second is a lure pile.
- **The chord-consonance cluster (~5×)** and **the manuscript block** — the cluster stays a closed tail (no sixth); the manuscript quartet just aged down to 3× on its own, so its bans self-released. Neither needs action beyond "don't feed them."
- **16896-strata** — still verification-hostile (payoff invisible on first visit). Do not lead a morning with it. Unchanged for the fourth verdict.

## Provocations for tomorrow's dream cycle
1. **Verification is the only lever, unchanged at `0 / 15 / 0` for ~12 days — and now there are two doors to it. Consolidate to ONE.** Lead the morning with **`17104-firstlook`** (auto-loops on open, no press-play, no permission, works muted, ~10 s: does the ink swell black on his forte and thin to a dry hair on his lift?). Make inkpressure the "then see the whole piece," not a second ask. One tap, one door.
2. **Do not build a second lure or a third front door.** firstlook was the one legitimate lure-build the last verdict sanctioned. A second preview is inventory wearing bait's coat.
3. **When building resumes, ship `breathline` — but off the tags firstlook re-concentrated.** The queued non-chord time-feel pick (rubato as a slow ~0.36 Hz *breathing* pulse, bioRxiv Mar-2026, grep-verified unbuilt) is right and its research is now sharp. Ship it **audio-forward or Canvas2D (both rested), NOT SVG (now 3×), NOT three.js (3×)**, and **warm/neutral, NOT cool-luminous (now 5×)**. The manuscript bans have released, so DOM-typographic is even available again — but a fourth non-chord concept-break matters more than reusing a freed renderer.
4. **Turn the lens on this jury. Thirteen verdicts, ceiling unmoved — the daily critique is now the clearest piece of inventory in the lab.** I made this verdict shorter than the last on purpose; the honest next step is shorter still. On a frozen-board morning the jury should collapse to two lines (the audit delta + the one ask) and nothing else. An elaborate page of taste-criticism that Karel may not be reading is exactly the pile every recent verdict has told the *builder* to stop making. Physician, heal thyself.
5. **The diversity board is releasing organically and it still does not matter.** Four manuscript bans dropped this rotation with no action from the agent; cool-luminous rose for a defensible reason. Good and bad, both orthogonal to the ceiling. Stop reading the audit as a scoreboard.

## Karel-facing line
Everything you asked the lab to try it has now tried — it built the one-glance lure, then had the discipline to build nothing — and twelve days later the whole board still turns on a single tap: open **firstlook** (10s, auto-loops the moment it opens, no press-play, works muted on your phone) and tell me if the ink swells black when he leans in and thins to a hair when he lifts. One "yes" is worth more than everything else here.
