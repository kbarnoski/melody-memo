# Concept Jury Verdict — 2026-09-06

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/keyboard/touch/data/peer allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (e1e3a05→f7aa0db; cycle 1221 `16944-nodalfigure` at HEAD). Read-only on `src/app/dream/**` — no prototype touched. **TWO new ships since the 2026-09-05 verdict** (which reviewed through `16848-tunevoice`): `16896-strata` (c1220, DEEP, WebGPU cross-session stratigraphy) and `16944-nodalfigure` (c1221, WIDE, SVG just-intonation harmonograph). Window rotated by **two**: `manyreturns` **and** `phosphene` aged out; `strata` + `nodalfigure` entered. RESEARCH.md is fresh (dated 2026-09-06 entry for c1221; the research→build chain is visible). The consequence that dominates this verdict: **the lone 4 (`manyreturns`) has now rotated OUT of the 15-window.** The top ambition band is empty for the first time on record._

## Summary
The lab did the disciplined thing twice in a row and it shows: it **heeded yesterday's #2 warning verbatim** — no fourth raw-WebGL2 field. It went WebGPU (`strata`), then **SVG vector ink** (`nodalfigure`) — a surface the lab has *never* used. As a direct result, raw-WebGL2 held at 3× instead of becoming the next Canvas2D, and Canvas2D itself fell out of ban range (4×→3×) purely because nothing new landed on it. Surface diversity is now genuinely, measurably healthy. **But** the ceiling didn't move — and this window it got worse in a way no prior verdict could say: `manyreturns`, the lone 4, aged out, so the ambition distribution is now a flat **`0 / 15 / 0`** — fifteen consecutive honest 3s and, for the first time, **not a single 4 or 5 anywhere in the window.** Eight days at zero verified 5s. And beneath the diversifying renderers there's a rut the *tag* audit is structurally blind to: four of the recent builds (`harmonicswarm`, `tunevoice`, `strata`, `nodalfigure`) are all the same **concept** — *make the chord's consonance/tension perceptible.* The lab escaped the renderer monoculture and quietly built a **concept** monoculture in its place. The build machine is still a flawless perpetual-motion honest-3 generator; it is now generating ready-but-unverified inventory faster than one human can ever cash it.

## Diversity audit
_(last 15, numerically highest on disk: driftchoir, terrafret, spectralbraid, longvigil, tonguescript, scriptorium, albumvoyage, cadencemap, chordnebula, harmonicswarm, attune, gushbody, tunevoice, strata, nodalfigure — `manyreturns` + `phosphene` aged out; `strata` + `nodalfigure` entered)_

- **Over-represented input:** **`keyboard / typing` — still 4×, still ban-floored** (tonguescript, scriptorium, albumvoyage, cadencemap — the manuscript block, byte-identical to the last three verdicts; none aged out). **NEW cluster worth naming before it hardens: `pointer/touch drag` is now 4×** (chordnebula pointer/tilt, harmonicswarm pointer/tilt, strata core-sample scrub, nodalfigure touch-perturb). Two of those (strata, nodalfigure) are *secondary* layers over catalog playback, so strictly-primary pointer is only 2× — but as an *interaction gesture*, "drag on the thing" now appears in 4 of 15. This is exactly how a rut starts. Singletons/thin (healthy): camera (gushbody 1×), voice/mic (tunevoice 1×), multi-user peer (attune 1×), live-external-data (terrafret 1×), pen-paint (spectralbraid 1×), autonomous/passive (longvigil, driftchoir 2×). **BANNED next cycle: `keyboard/typing` (4×). WATCH (avoid — 4× as a gesture): `pointer/touch drag`** — rest the pointer next fire.
- **Over-represented output:** **The renderer picture is the good news of this verdict.** `DOM/typographic` holds **4×** (the four manuscripts — frozen, banned). Everything else moved the right way: **`Canvas2D` 4×→3×** (terrafret, spectralbraid, longvigil — manyreturns aged out) → **ban DROPPED, now WATCH.** **`raw WebGL2 fragment field` held at 3×** (attune, gushbody, tunevoice — the exact three from yesterday; nothing new joined) → the #2 warning was *obeyed*, it did not become 4×. `WebGPU` **1×→2×** (harmonicswarm compute, strata render) — WATCH, do not make 3×. `three.js` **2×→1×** (chordnebula only; phosphene aged out) — now the most rested GPU surface in the lab. `SVG vector ink` **1×** (nodalfigure — brand-new surface). `audio-only` 1× (driftchoir). **BANNED next cycle: `DOM/typographic` (4×). WATCH: `Canvas2D` (3×), `raw WebGL2 fragment field` (3× — still one ship from a ban, keep it there), `WebGPU` (2× — do not make 3×).**
- **Over-represented technique:** `prosody / text-score cross-modal mapping` — **still 4×** (the four manuscripts), banned, unmoved. `loops-of-one-take` dropped to **1× direct** (driftchoir; manyreturns aged out) but the four manuscripts still run incommensurate-loop phasing as substrate → ~5× — **keep hard-closed.** The singletons to preserve and extend (do NOT re-ban): cross-session persistent accretion / stratigraphy (strata), just-intonation harmonograph / Chladni figure (nodalfigure), YIN pitch→consonance reward (tunevoice), GPU particle-life w/ chord attraction (harmonicswarm), Horn–Schunck optical-flow feedback (gushbody), co-presence attunement scalar (attune), volumetric FBM raymarch (chordnebula), spectral cross-synthesis (spectralbraid), stateful memory-accretion (longvigil), live-data sonification (terrafret). **BANNED next cycle: `prosody/text-score` (4×) + `loops-of-one-take family` (keep closed).**
- **Over-represented vibe:** `near-dark / intimate typographic` — **still 5×** (driftchoir + the four manuscripts), banned, unmoved (all five in-window). `warm / ember intimate` holds **2×** (attune, gushbody) — WATCH. `bright cosmic field` holds **2×** (chordnebula, harmonicswarm) — WATCH. `two-signal duotone` 2× (terrafret, spectralbraid). **NEW and welcome: `cool luminous` is the clean break the last two ships both took** — strata's cool mineral / core-sample tones and nodalfigure's cool cyan→violet vector light (2×, fresh, not near ceiling). **BANNED next cycle: `near-dark/intimate typographic` (5×). WATCH: `warm/ember` (2×), `bright cosmic field` (2×).**
- **BANNED for next cycle:** `keyboard/typing input` (4×) + `DOM/typographic output` (4×) + `prosody/text-score technique` (4×) + `loops-of-one-take family` (keep closed) + `near-dark/intimate typographic palette` (5×). **WATCH (at/near ceiling — AVOID):** `pointer/touch drag input` (4× as a gesture), `Canvas2D output` (3×), `raw WebGL2 fragment field output` (3×), `WebGPU output` (2×), `warm/ember` + `bright cosmic field` palettes (2×). **Honest headline: the tag audit is now the lab's success story and its blind spot at the same time.** Every renderer count moved the right direction and the raw-WebGL2 rut was caught and held at 3× — real discipline. But the manuscript block (keyboard/DOM/prosody/near-dark) is *still* pinned at 4/4/4/5 because none of the four aged out, and the audit's four axes cannot see that the last four ambitious ships are the same *idea* wearing four different renderers (see Summary + Provocation #2).

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Twenty-second straight fortnight with an empty floor. Solved long ago; stop watching it.
- **Hit 2–3 criteria — 15** (driftchoir 2, terrafret 2, spectralbraid 3, longvigil 3, tonguescript 3, scriptorium 3, albumvoyage 3, cadencemap 3, chordnebula 3, harmonicswarm 3, attune 3, gushbody 3, tunevoice 3, **strata 3, nodalfigure 3**). Both new ships are honest, disciplined 3s: **strata** cleared #2 (7 subsystems: catalog + FFT/RMS + chord tracker + per-session summary extractor + cross-session IndexedDB store + core-sample timeline + WGSL render pipeline) + #3 (arXiv:2607.21686 PCS / Katie Paterson *Future Library*) + #5 (RESEARCH §2026-09-05 c1220), and correctly **declined #1** (spiralkeep predates persistence). **nodalfigure** cleared #2 (6 subsystems: catalog + FFT/RMS + chord tracker + just-intonation ratio engine + SVG renderer + touch-perturb) + #3 (Chladni *Klangfiguren* / Victorian harmonograph / VectraSynth 2026) + #5 (RESEARCH §2026-09-06 c1221), and correctly **declined #1** (291-harmonograph predates). Honest-first discipline now holds **seven ships running** — that habit is fully internalized; stop congratulating it.
- **Hit 4–5 criteria — 0.** Distribution **`0 / 15 / 0`.** This is the sentence that matters: the lone 4 (`manyreturns`) that the last three verdicts kept naming as the obvious deepen target has now **aged out of the window entirely** — so the top band, which used to hold one point, holds **zero**. Fifteen consecutive honest 3s. **Eighth straight day at zero verified 5s.** Every one of the fifteen was shipped stamped "not run in this env — needs Karel at the page." The load-bearing fact, now unarguable: the lab has cashed WebGPU-compute, WebRTC/multi-user, camera/optical-flow, voice/audio-forward, cross-session-persistence AND SVG across this window — six fresh categories — and the top band went from 1 to **0**. Surface was never the gate. The build lever moves the *diversity* number and has never once moved the *ceiling*; this window it let the last remaining ceiling point rotate off the board.

## Standouts (positive)
- **16944-nodalfigure** — the best single *concept-sentence* the lab has produced in weeks: *"consonant harmony draws a clean closed loop; tension frays it into a drifting web,"* rendered in living SVG. It is also, for a concrete reason, **the fastest eye-verdict Karel can give** — pure `<svg>`, so no WebGL, no WebGPU, no camera, no mic, no permission prompt, no headphones; it renders identically on his phone and reads in ~15 seconds. If exactly one piece gets opened tomorrow, this is the eye-call and `tunevoice` is the ears-call. Honest caveat the agent itself flagged: the closure-vs-fray threshold is *hand-tuned and never heard against real music* — so the risk is that it's too subtle to read. That risk is precisely *why it must be verified first*, not built past.
- **16768-harmonicswarm** — still the technical high-water mark of the window: the lab's only raw-WebGPU-compute piece, Karel's chord rewriting a 6×6 inter-species attraction matrix so consonance is literally *visible as physics*. Phone-renderable now WebGPU is default on iOS 26. The strongest eye-call after nodalfigure.
- **16896-strata** — the most *ambitious* build of the two new ones (7 subsystems, genuine cross-session persistence, a concept — a recording that remembers being heard — with real depth). Naming it here for the ambition. But see Pruning: its verification cost is the highest in the backlog, which is the wrong shape for the moment the lab is in.
- **16848-tunevoice** — still the single most valuable piece for one reason unchanged since yesterday: it is the **fastest ears-verdict** (a ~20-second solo auto-demo, mic optional). The eye-calls and the ears-call between them are the whole ballgame.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **The manuscript block** (tonguescript → scriptorium → albumvoyage → cadencemap, all 3/5) — unchanged structural drag, and the reason four of the five bans (keyboard, DOM, prosody, near-dark) *cannot* drop. The window advanced by two this cycle but the manuscripts are 16656–16720 and still sit comfortably inside the top 15; it will take **~4 more ships above 16720** to push the first one out. Until then these four bans are pinned. Do not add a fifth manuscript under any framing.
- **The "make the chord's consonance/tension visible" concept cluster** (harmonicswarm → tunevoice → strata → nodalfigure — ~4×) — flagging this as the *real* similarity note this verdict, the one the tag audit is blind to. Each wears a different, healthy renderer (WebGPU / WebGL2 / WebGPU / SVG) and a different input, so they pass every diversity check — yet all four answer the same question: *"can you perceive the harmony/tension of this chord?"* That is a genuinely good question, and it is now the lab's comfort zone. This is the deeper version of Karel's original "too similar in theme." Break it at the *concept* level next fire (Provocation #2), not just the renderer level.
- **16896-strata** (3/5) — concept-strong, **verification-hostile.** Its entire payoff — accreted sediment from *multiple past sessions* — cannot be seen on a first visit; it requires Karel to return across sessions before it shows anything. That is the exact opposite of the 15–20-second phone test the ceiling needs right now. A wonderful idea shipped into a moment that can't afford it. Do not lead the morning with it.
- **16496-terrafret** (2/5), **16464-driftchoir** (2/5) — the settled honest-2s: disciplined, read well, reach for nothing (data-sonification duotone; audio-only). Fine as rested-surface breathers, not concepts to extend toward a 5.

## Provocations for tomorrow's dream cycle
1. **Verification is STILL the only lever — and it is now the ONLY thing standing between the lab and an all-3s window with the lone 4 gone.** The top band is empty. Nothing new should ship until Karel opens ONE ready piece. Lead the morning with the two fastest verdicts, framed as a choice of 30 seconds total: **eye — `nodalfigure`** (SVG, no permission, phone-identical, ~15s: does the figure visibly *close* on his consonant chords and *fray* on the tense ones?); **ears — `tunevoice`** (~20s auto-demo, mic optional). A single "yes, that's a 4" unfreezes eight days of frozen ceiling; a sixteenth honest-3 does not.
2. **Break the CONCEPT rut, not just the renderer rut.** Four of the last ambitious builds all ask "can you perceive this chord's consonance/tension?" (harmonicswarm, tunevoice, strata, nodalfigure). Renderer diversity is healthy; *conceptual* diversity is not. Next ambitious concept must target a **different musical dimension** — rhythm / rubato / time-feel, or form & structure (the shape of a whole piece, not a single chord), or touch-dynamics / timbre, or something non-analytic entirely (space, silence, embodiment). Stop asking "show me the chord."
3. **Renderer diversity worked — bank the win and keep it.** raw-WebGL2 was held at 3× (you obeyed the warning) and Canvas2D fell out of ban. `three.js` is now the most *rested* GPU surface (1×) — the banked **`16928-resonarium`** (three.js, an accreting Xenakis cathedral) is safe to ship AND breaks the consonance-concept rut (it's about *architecture/form*, not consonance). Ship it there. But do NOT make a 4th raw-WebGL2 field, and WebGPU is now 2× — don't make it 3×.
4. **Stop growing the ready-pile faster than one human can cash it.** The lab is holding ~6 unverified eye/ears calls (nodalfigure, strata, gushbody, attune, harmonicswarm, chordnebula, tunevoice) *plus* ~7 banked seeds (resonarium, innervoice, reliquary, touchtone, deepcaustic, tether, tidalchord). The bottleneck is not ideas. If Karel still hasn't verified anything by the next fire, consider a fire that ships **nothing new** and instead makes ONE existing piece un-missable — a solo auto-demo, a 10-second "here's the effect" preview loop, a phone-first fallback. That is a more valuable use of a cycle right now than a sixteenth 3.
5. **Rest the pointer.** `pointer/touch drag` is quietly 4× as a gesture (chordnebula, harmonicswarm, strata, nodalfigure) — the emerging input rut now that keyboard is aging out. Next input should be audio-forward (thin at 1×, and it's an *ears* verdict), autonomous/ambient, or one of the banked non-pointer surfaces (voice, co-presence).

## Karel-facing line
You did the right thing on the machinery — SVG is a brand-new surface, and you caught the raw-WebGL2 rut and held it — but the last honest 4 just rotated off the board, so the whole window is now 3s with nothing verified, and the real sameness isn't the renderer anymore, it's that your last four big builds all just ask "can you feel this chord?" — so open **nodalfigure** (15 seconds, no permissions, works on your phone) or **tunevoice**, give me one real verdict, and then let's chase a different question than the chord.
